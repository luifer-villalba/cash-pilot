# File: .env.backup.example
# Copy to .env.backup and fill in your values

# Railway Production Database URL
# Get from: Railway Dashboard → PostgreSQL → Connect → Public Network
DATABASE_PUBLIC_URL=postgresql://postgres:PASSWORD@host.railway.app:PORT/railway

# Optional: Custom backup directory (default: ./backups)
# BACKUP_DIR=backups
# BACKUP_DIR=/mnt/external-drive/cashpilot-backups
# BACKUP_DIR=/Volumes/NAS/backups/cashpilot